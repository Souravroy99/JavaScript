const arr = [1,2,3,4,5]

arr[3] = 100
arr[10] = 999

console.log(arr);

// We can change the value, but we can't change the reference


// Reassignment is not allowed
// Changing reference(Error): arr = [x, y, z]